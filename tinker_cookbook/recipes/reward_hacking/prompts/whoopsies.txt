I have received the response.

I am now trying to write an LLM-as-judge to act as a reward model to guide the RL on the user turn strategy that I want to try. However right now what is in llm_judge.py makes the model's go fairly degenerate, just saying one sentence that makes it really easy.